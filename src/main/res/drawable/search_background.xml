<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="18dp" /><!-- Adjust the radius for the roundness -->
            <solid android:color="#FF5722" /> <!-- Button color when pressed -->
        </shape>
    </item>

    <!-- Focused state -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="18dp" />
            <solid android:color="#FF9800" /> <!-- Button color when focused -->
        </shape>
    </item>

    <!-- Default/Normal state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="18dp" />
            <stroke android:width="1dp" android:color="#B5B5C7"/><!-- Default button color -->
        </shape>
    </item>

</selector>
